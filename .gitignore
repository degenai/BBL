# Secrets / env
.env
.env.*
!.env.example
secrets.local
*.key

# Local machine-specific Claude Code config (permission allowlists, etc.)
.claude/settings.local.json

# Card image cache — regeneratable via researchbot.py --prepare-only
# Untracked 2026-05-23 to keep repo under GitHub's 2 GB pack-size limit;
# history scrubbed via git filter-branch in the same commit
cards/_images/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Logs / scratch
*.log
scratch/

# Triviabot agent curl-cache scratch (sandbox can't delete on exit)
reports/.cache_*

# LorcanaJSON bulk allCards cache (7.6 MB, re-downloads on demand)
reports/lorcana_allcards.json

# Probe scratch (subagent feasibility-investigation artifacts; throwaway, regeneratable)
tmp_probe/

# Parent-side Puppeteer / curl cache (web-scrape artifacts from triviabot research)
.cache/

# Obsidian per-user dev state (graph view position, pane layout — not session work)
cards/.obsidian/workspace.json
cards/.obsidian/workspace-mobile.json
*.tmp
